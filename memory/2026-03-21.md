
## RAG 知識庫 SOP 升級：自動索引標籤
- **事件**：為 `/Volumes/2T/Docling_Workspace/output_mds/` 內既有的 10 本 Markdown 檔案，於第一行加入 `# 核心標籤：關鍵字1, 關鍵字2...`，大幅提升 RAG (`memory_search`) 的向量檢索精準度。
- **未來執行承諾**：
  1. 接收到新 PDF 並完成 Docling 轉譯為 Markdown 後。
  2. 除了依據「領域命名空間」歸檔至對應資料夾外。
  3. **必須主動幫新檔案生成並寫入該書本/文件的「核心關鍵字標籤」於首行**，再回報 Erick 完成。
- **目的**：確保 Qwen 2.5 14b 大腦能透過標籤瞬間定位知識領域，不需 Erick 手動操作。
